# kind cluster config for local kubetidy testing.
# Single control-plane node is enough to exercise discovery, metrics-server and a scan.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kubetidy
nodes:
  - role: control-plane
